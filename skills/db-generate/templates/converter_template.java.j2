/**
 * Generated by skill: {{ generator_name }} v{{ generator_version }}
 * Since: {{ generated_at }}
 * Do not modify directly; regenerate via skills if changes are needed.
 * If manual changes are necessary, document @version, @since and change reasons in skill docs.
 */
package {{ package }};

import {{ entity_package }}.{{ entity_name }};
import {{ dto_package }}.{{ dto_name }};

/**
 * {{ entity_name|replace('Entity','') }}对象转换工具
 */
public final class {{ converter_name }} {

    private {{ converter_name }}() {}

    public static {{ dto_name }} toDTO({{ entity_name }} e) {
        if (e == null) return null;
        {{ dto_name }} d = new {{ dto_name }}();
        {% for f in fields %}
        d.set{{ f.method_suffix }}(e.get{{ f.method_suffix }}());
        {% endfor %}
        return d;
    }

    public static {{ entity_name }} toEntity({{ dto_name }} d) {
        if (d == null) return null;
        {{ entity_name }} e = new {{ entity_name }}();
        {% for f in fields %}
        e.set{{ f.method_suffix }}(d.get{{ f.method_suffix }}());
        {% endfor %}
        return e;
    }
}
