package {{ package }};

import com.baomidou.mybatisplus.annotation.EnumValue;
import com.fasterxml.jackson.annotation.JsonValue;
import lombok.AllArgsConstructor;
import lombok.Getter;

/**
 * {{ enum_comment }}
 *
 * Generated by skill: enum-gen
 */
@Getter
@AllArgsConstructor
public enum {{ enum_name }} {

    {% for item in items %}
    /**
     * {{ item.label }}
     */
    {{ item.name }}({{ item.code }}, "{{ item.label }}"){% if not loop.last %},{% else %};{% endif %}

    {% endfor %}

    @EnumValue
    private final {{ code_type }} code;

    @JsonValue
    private final String desc;
    
    public static {{ enum_name }} of({{ code_type }} value) {
        for ({{ enum_name }} item : values()) {
            if (item.code.equals(value)) {
                return item;
            }
        }
        return null;
    }
}
